package creational.bridge.shape;

// doesn't implement the bridge pattern, instead uses inheritance
public class Shape1BridgeDemo {

    public static void main(String[] args) {

	// Problem is that adding colors & shapes
	// means that the number of classes would explode
//	Circle circle = new BlueCircle();

//	Square square = new RedSquare();

//	circle.applyColor();
//	square.applyColor();
    }
}
